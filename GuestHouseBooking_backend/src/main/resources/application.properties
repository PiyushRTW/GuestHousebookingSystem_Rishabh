spring.application.name=GuestHouseBooking

spring.datasource.url=jdbc:mysql://localhost:3306/guest_house_booking
spring.datasource.username=root
spring.datasource.password=piyush@8457
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.jpa.show-sql=true
!keep true if you want to log the performed sql queries

spring.jpa.properties.hibernate.format_sql=true
!format the SQL output in the console, making it more readable

# Spring Mail Configuration
spring.mail.host=smtp.gmail.com
# Example for Gmail SMTP host
spring.mail.port=587
# Example for Gmail port (TLS)
spring.mail.username=piyushkumarrathwa.svma@gmail.com
 # Your email address (e.g., used to send emails)
spring.mail.password=piyush#lion
  # Your email password or app-specific password (if using Gmail, generate an App Password)
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
app.mail.admin-email=rathwapiyush8457@gmail.com
app.mail.app-base-url=http://localhost:8080
app.mail.admin-pending-bookings-path=/admin/bookings/pending